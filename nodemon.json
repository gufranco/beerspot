{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts"],
  "exec": "npm run build && node --inspect ./dist/app.js",
  "env": {
    "NODE_ENV": "development",
    "BCRYPT_ROUNDS": 10,
    "HTTP_PORT": 8080,
    "TYPEORM_HOST": "localhost",
    "TYPEORM_PORT": 5432,
    "TYPEORM_USERNAME": "root",
    "TYPEORM_PASSWORD": "root",
    "TYPEORM_DATABASE": "mandabrejas",
    "JWT_PASSWORD": "fakePassword"
  }
}
